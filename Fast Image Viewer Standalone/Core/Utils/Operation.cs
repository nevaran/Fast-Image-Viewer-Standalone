using System;

namespace FIVStandard.Utils
{
    /*public class Operation : IDisposable
    {
        private MainWindow _mainWindow;

        public bool CanProceed { get; }

        public Operation(MainWindow mainWindow)
        {
            _mainWindow = mainWindow;
            CanProceed = mainWindow.!IsOperationPending;

            if (CanProceed)
                _mainWindow.IsOperationPending = true;
        }

        public void Dispose()
        {
            if (CanProceed)
                _mainWindow.IsOperationPending = false;
        }
    }*/

    //private Operation StartOperation() => new Operation(this);
}